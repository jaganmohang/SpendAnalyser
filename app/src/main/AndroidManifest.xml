<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.jagan.spendanalayser">

    <application
            android:name=".ETApp"
            android:allowBackup="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="false"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning"
            tools:replace="android:allowBackup,android:supportsRtl">
        <activity android:name=".ui.splash.SplashActivity" android:screenOrientation="sensorPortrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.budget.BudgetActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".ui.categories.CategoriesActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".ui.MainActivity" android:screenOrientation="sensorPortrait"/>
    </application>

</manifest>